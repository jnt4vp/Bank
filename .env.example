APP_ENV=development
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/bank
JWT_SECRET=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
SQL_ECHO=false
AUTO_CREATE_TABLES=false
# JSON array or comma-separated list
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# LLM classifier (Ollama)
OLLAMA_ENABLED=true
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:1b
# Goals LLM batches: seconds per HTTP call, max txns per request (smaller batch = faster each call)
OLLAMA_TIMEOUT=60
OLLAMA_MAX_BATCH=3

# Gmail SMTP — alert emails
GMAIL_USER=your-sending-account@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx
ALERT_EMAIL=fallback@example.com

# Plaid — get credentials at https://dashboard.plaid.com
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
PLAID_POLL_INTERVAL_MINUTES=30
PLAID_TOKEN_KEY=

FRONTEND_URL=http://localhost:5173

# Demo: persist “savings transfers” without ACH/Plaid money movement (see SimulatedSavingsTransfer).
SIMULATED_TRANSFERS_ENABLED=true

# Development seed user
DEV_SEED_EXAMPLE_USER=true
DEV_SEED_EXAMPLE_NAME=Test User
DEV_SEED_EXAMPLE_EMAIL=test@example.com
DEV_SEED_EXAMPLE_PASSWORD=Password123!

# Optional: used by Settings → Contact support (mailto). Place in frontend/.env for Vite.
# VITE_SUPPORT_EMAIL=team@your-school.edu
